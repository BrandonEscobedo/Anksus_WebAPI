@inject anskus.Application.Cuestionarios.ICuestionarioService _CuestionarioService
@inject IDialogService DialogService
@inject anskus.Application.Cuestionarios.IIdContainer IdContainer
@using System.Security.Claims
@using MediatR
<MudDialog>
<DialogContent>
    <MudInputLabel>Titulo</MudInputLabel>
    <input type="text" @bind-value="cuestionarioDTO.Titulo" />
@*     <MudSelect @bind-Value="cuestionarioDTO.IdCategoria" Label="Categoria">
@foreach (var item in listaCategorias)
{
    <MudSelectItem Value="@item.IdCategoria">@item.Categoria</MudSelectItem>
}
</MudSelect> *@
</DialogContent>
<DialogActions>
        <MudButton OnClick="hide">Cancel</MudButton>
    <MudButton Color="Color.Primary" OnClick="GuardarEditarCuestionario">Ok</MudButton>
</DialogActions>
</MudDialog>
@code {
    [CascadingParameter] MudDialogInstance MudDialog { get; set; }
    void hide() => MudDialog.Cancel();
    private ISender? _mediator;
    [Inject] AuthenticationStateProvider? AuthStateProvider { get; set; }


    public CuestionarioDTO cuestionarioDTO { get; set; } = new CuestionarioDTO();
    public List<CategoriasDTO> listaCategorias = new List<CategoriasDTO>();
    protected override async Task OnInitializedAsync()
    {
     

        cuestionarioDTO.IdCuestionario = IdContainer.idCuestionario;
        // listaCategorias = await _CategoriasService.GetAllCategorias();
    }
    public async Task GuardarEditarCuestionario()
    {
        if (IdContainer.idCuestionario == 0)
        {
            var result = await _CuestionarioService.Add(cuestionarioDTO);
            if (result !=0)
            {
                IdContainer.SetIdCuestionario(result);
                cuestionarioDTO.IdCuestionario = IdContainer.idCuestionario;
                hide();
            }

        }
        else
        {
            var result =   await _CuestionarioService.Update(cuestionarioDTO);
            if (result)
                hide();
        }

    }
}
